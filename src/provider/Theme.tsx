import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#5b46ff',
    },
    secondary: {
      main: '#f0efff',
    },
    background: {
      default: '#f5f7fa',
      paper: '#ffffff',
    },
  },
})

export default theme